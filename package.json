{
  "name": "aarg-ebay-price-analytics",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "eBay SRP price scraper userscript, with a UI-drift canary watched by the triage harness",
  "scripts": {
    "canary:live": "node canary/live_canary.mjs",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "capture-fixture": "node scripts/capture-fixture.mjs"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "playwright-core": "^1.56.0"
  }
}
